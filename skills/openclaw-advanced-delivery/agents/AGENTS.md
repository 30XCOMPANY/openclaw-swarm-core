# agents/
> L2 | 父级: /Users/oogie/.openclaw/workspace/openclaw-swarm-core/skills/openclaw-advanced-delivery/AGENTS.md

成员清单
- AGENTS.md: `agents/` 子目录地图。
- openai.yaml: `openclaw-advanced-delivery` 的 UI 元数据与默认 prompt。

法则
- `agents/` 只放机器读取的界面元数据。
- 元数据必须体现 advanced delivery 而不是 direct coding。

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
