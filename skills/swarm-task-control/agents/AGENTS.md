# agents/
> L2 | 父级: /Users/oogie/.openclaw/workspace/openclaw-swarm-core/skills/swarm-task-control/AGENTS.md

成员清单
- AGENTS.md: `agents/` 子目录地图。
- openai.yaml: `swarm-task-control` 的 UI 元数据与默认 prompt。

法则
- `agents/` 只放机器读取的界面元数据。
- 元数据必须体现“已在 Swarm 中的任务控制”，不能误导成模式选择。

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
